services:
  nginx:
    image: lscr.io/linuxserver/swag:latest@sha256:d48a91101c5ec0cb32657ac1717a9c1d3bfc36024ec6500f7e7a8de0a871ccfd
    container_name: nginx
    cap_add:
      - NET_ADMIN
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - URL=about.bl4ko.com
      - VALIDATION=http
      # - SUBDOMAINS=www, #optional
      - STAGING=false #optional
    volumes:
      - ./docker/swag:/config
    ports:
      - 443:443
      - 80:80 #Required for certbot validation
    restart: unless-stopped
    depends_on:
      - next

  next:
    image: bl4ko/homepage:latest@sha256:36ef918bdb8fc760744be2d61429701e55de1cf01670c5cf76cd95067195fb4c
    container_name: next
    expose:
      - 3000
