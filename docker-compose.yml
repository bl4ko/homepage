services:
  nginx:
    image: lscr.io/linuxserver/swag:latest@sha256:5b0bf6f7050b3bd28058ca9861cc0aa774842bfb120ef7346fa00cac8b2e72f4
    container_name: nginx
    cap_add:
      - NET_ADMIN
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - URL=about.bl4ko.com
      - VALIDATION=http
      # - SUBDOMAINS=www, #optional
      - STAGING=false #optional
    volumes:
      - ./docker/swag:/config
    ports:
      - 443:443
      - 80:80 #Required for certbot validation
    restart: unless-stopped
    depends_on:
      - next

  next:
    image: bl4ko/homepage:latest@sha256:22bc73909019828f97f3df58c735a05cfc1234f5964e9d94b43f1e6ce8de138f
    container_name: next
    expose:
      - 3000
