name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@89fdb71a17397a2747a1fe0300a6b73476ff3576 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
