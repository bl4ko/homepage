name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@3357bb7564bb5080b048628f22c8b3fc0dc1d337 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
