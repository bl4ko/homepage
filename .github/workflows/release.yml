name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@37e2500100b0b94ba2135342e1d9f6f2054a6019 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
