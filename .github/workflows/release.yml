name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@1787d6ebe7a7d752d2ce9f4d254c8d962b4c8670 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
