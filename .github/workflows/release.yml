name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@b1ba6cb283691e0acf7d0e942a2eda425c8bb8ac # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
