name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@754867d593ffd69259b840561ad847396300760d # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
