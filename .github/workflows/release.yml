name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@cbecb42ab55d8ca7a168020d52b20c1df83db1b2 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
