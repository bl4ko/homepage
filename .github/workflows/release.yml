name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@a4d8c199a3829fdcd38f144d8ddd4a07b7f91bdf # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
