name: Create Release 🚀
permissions:
  contents: read
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: bl4ko/shared-actions/.github/workflows/release.yml@33aaba12a83e3c7b5f9f854d591645501e57bb19 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
