name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@da22f11edd90350b65a5152b5a88703bb7a7c2be # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
