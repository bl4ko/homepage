name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@b1ba6cb283691e0acf7d0e942a2eda425c8bb8ac # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
