name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@89fdb71a17397a2747a1fe0300a6b73476ff3576 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
