name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@1787d6ebe7a7d752d2ce9f4d254c8d962b4c8670 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
