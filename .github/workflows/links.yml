name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@754867d593ffd69259b840561ad847396300760d # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
