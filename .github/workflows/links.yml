name: Links
permissions:
  contents: read
on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"

jobs:
  linkChecker:
    uses: bl4ko/shared-actions/.github/workflows/links.yml@3357bb7564bb5080b048628f22c8b3fc0dc1d337 # main
    secrets:
      SVC_TOKEN: ${{ secrets.SVC_TOKEN }}
